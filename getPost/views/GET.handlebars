<h1>GET Request Received!</h1>
<table>
	<thead>
  		<tr>
   			{{#each dataList.[0]}}
      			<th>{{@key}}</th>
    		{{/each}}
  		</tr>
	</thead>
	<tbody>
  		{{#each dataList}}
    		<tr>
    			{{#each this}}
        			<td>{{this}}</td>
      			{{/each}}
    		</tr>
  		{{/each}}
	</tbody>
</table>
<ul>
  {{#each dataList}}
    <li>{{this.name}}: {{this.value}}
  {{/each}}
</ul>